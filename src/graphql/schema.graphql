### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Car {
  brand: String!
  description: String!
  id: ID!
  image: String!
  name: String!
  price: Int!
  stock: Int!
  transaction: [Transaction!]!
}

type CarHistory {
  amount: Int!
  car: Car!
  id: ID!
  id_car: String!
  id_history: String!
  price: Int!
}

type History {
  id: ID!
  total: Int!
}

type Mutation {
  addCarHistory(amount: Int!, id_car: String!, id_history: String!, price: Int!): Boolean
  addTransaction(amount: Int!, car_id: String!, username: String!): Boolean
  amountTransaction(amount: Int!, id: String!, isIncrement: Boolean!): Boolean
  deleteTransaction(id: String!): Boolean
  saveToHistory(tanggal: String!, total: Int!, transaction_ids: [String!]!): Boolean
}

type Query {
  car: [Car!]
  car_history(id_history: String!): [CarHistory!]
  transaction(username: String!): [Transaction!]
}

type Transaction {
  amount: Int!
  car: Car!
  car_id: String!
  id: ID!
  user_id: String!
}